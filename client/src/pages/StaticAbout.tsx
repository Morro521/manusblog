import { ArrowUpRight, Github } from "lucide-react";
import { siteLinks } from "@/data/site";
const facts = [["01", "PERSONAL SITE", "Morro 主页保留日常入口、博客与 GitHub 的访问路径。"], ["02", "TECHNICAL NOTES", "MorroBlog 公开记录 Cloudflare、Docker、NAS、Home Assistant、Hexo 与网络实验。"], ["03", "STATIC BY DESIGN", "本站不需要数据库、认证、对象存储或 API；内容与图像均随 GitHub 提交版本化。"], ["04", "READ THE SOURCE", "首页与索引只做定位，每条引用都保留到既有 MorroBlog 原文的出口。"]] as const;
export default function StaticAbout() { return <section className="about-page"><div className="about-page__hero"><p className="signal-label">ABOUT / A PERSONAL TECHNICAL STATION</p><h1 className="display-title">不是信息流。<br />是一份<span>可回看的技术档案。</span></h1><p className="lede">MorroBlog 把个人主页的技术入口与既有博客记录合在同一条内容线上：自托管、网络、硬件与一次次实践之后留下的判断。</p></div><div className="fact-grid">{facts.map(([number, title, copy]) => <article key={title}><span>{number}</span><p className="signal-label">{title}</p><p>{copy}</p></article>)}</div><div className="about-page__statement"><div><p className="signal-label">EDITORIAL NOTE</p><p className="quote">“技术记录不需要显得很吵。它可以是一束刚好照到问题上的光。”</p></div><div><p>内容以原始博客为准，本站提供稳定的索引、阅读路线与外部出口。静态架构让每次更新都可以由 GitHub 版本记录、审阅和部署。</p><div className="about-page__links"><a href={siteLinks.home} target="_blank" rel="noreferrer">访问 Morro 主页 <ArrowUpRight size={16} /></a><a href={siteLinks.github} target="_blank" rel="noreferrer"><Github size={16} /> GitHub</a></div></div></div></section>; }
